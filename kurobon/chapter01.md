# 第1章　メモ📝
### 問１
- パッケージ
各クラスをグループ（パッケージ）に所属させて、分類・管理できるようにしている。
javaの識別子のルールに従っていれば自由に決めていい。アルファベットの小文字。
親子関係や階層関係はない。
同じパッケージ（無名パッケージ）に属しているクラスからしかアクセスできない。 -> コンパイルエラーになる。

- デフォルトパッケージ
どのパッケージに所属していない状態を「無題パッケージ」ともいう。

- 名前空間
使うことができる名前の総量。

### 問2

### 問3

### 問4

### 問5
- エントリーポイント
どのメソッドから処理を始めるかのポイント。
公開されていること（public）
インスタンスを生成しなくても実行できること（static）
戻り値はもどない（void）
メソッド名はmainであること
引数はString配列型を１つ受け取ること
引数には可変超引数のString型を受け取ることが出来ます -> コンパイル時に配列型の引数に変換される

### 問6

### 問7

### 問8
- javaコマンド
java 完全修飾クラス名 [引数 引数 ...]
mainメソッドに引数として渡される
jJVMを起動する
指定されたクラスをクラスパスから探し出してロードする
String配列型オブジェクトを作成し、起動パラメーターを格納する
起動パラメーターを保持したString配列型オブジェクトへの参照を引数に渡してmainメソッドを実行する

### 問9

- ClassNotFoundException
目的のクラスファイルを探し出せない。

- NoClassDefFoundError
クラス定義が見つからない。
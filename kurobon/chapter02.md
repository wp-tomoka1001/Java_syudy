# 第2章　メモ📝
### 問1
- リテラル
【プリミティブ型】

| データ型 | 値 |
|----------|------------------------------|
| boolean  | true, false                  |
| char     | 16ビットUnicode文字 \u0000 ~ \uFFFF |
| byte     | 8ビット整数 -128 ~ 127       |
| short    | 16ビット整数 -32768 ~ 32767  |
| int      | 32ビット整数 -2147483648 ~ 2147483647 |
| long     | 64ビット整数 -9223372036854775808 ~ 9223372036854775807 |
| float    | 32ビット単精度浮動小数点数   |
| double   | 64ビット倍精度浮動小数点数   |

### 問2
- 接尾後

| データ型 | 接尾後 |
|----------|------------------------------|
| long     | L |
| float    | F |

| ー | ー | 例 | 備考 |
|----------|------------------------------|---|---|
| 10進数     | 63 | ー | ー |
| 2進数    | 0b | 0b0111111 | ー |
| 8進数    | 0 | 077 | 0~7の数で表す |
| 16進数     | 0x | 0x3F | ー |

### 問3
- アンダースコア「-」
リテラルの先頭と末尾には記述できない
記号の前後には記述できない

### 問4
- 識別子
予約後の識別子として使うことはできない
使える記号は、アンダースコア「_」と＄マークのみ
数字から始めてはいけない

### 問5

### 問6
- ローカル変数（型推論）
変数が初期化されていなかったり、nullで初期化したりするなど、
データ型を特定できたい場合はコンパイルエラーになる。

### 問7
フィールドの宣言にはvarを使って型推論を使うとコンパールエラーとなる。
引数の型に型推論は使えない。

### 問8
B型の変数aにCクラスのインスタンンスを代入しようとしている為コンパイルエラーになる。

### 問9
- Stringクラス（オブジェクト）
newを使ってインスタンス化する
ダブルクォーテーション「”」で括った文字列リテラルを記述
valueOfメソッドを使ってStringインスタンスを生成する方法もある。

### 問10
- mutable（可変）なオブジェクト
フィールドの値を変更できるオブジェクト

- immutable（不変）なオブジェクト
フィールドの値を変更できないオブジェクト
全てのフィールドをprivateで修飾する
オブジェクト内部の状態を変更可能なメソッドは提供しない（例：setterメソッドを提供しない）
クラスをfinalで宣言し、メソッドがオーバーロードしないことを保証する（サブクラスからの変更を防ぐ）
内部に可変オブジェクトを保持している場合、そのオブジェクトを外部に提供しない（例:getterメソッドを提供しない）

String 変数名 = "文字列";
String 置換後の変数名 = 変数名.replaceAll("置換したい文字", "置換後の文字列")
Stringは不変。元の値を書き換えるではなく、新しくインスタンスを生成している。

### 問11
- charAt()メソッド
インスタンスが保持している値（文字列）から、引数で指定されている位置の文字を抜き出して戻す。
変数名.charAt(値)
文字番号の開始は0から開始する。
例外スローされる。

### 問1２
- indexOg()メソッド
指定した部分文字列が最初に出現するインデックスを返します。 
オプションで開始位置を取り、指定した数値以上のインデックスで指定した部分文字列が最初に出現するインデックスを返します。
要約すると、「ある文字列の中で、指定した文字列が最初に出現する位置を返す」というStringメソッドの１つです。
引数に指定した文字列がない場合は「-1」が返ってくる。

### 問13
- subtring()メソッド
substringは、文字列の一部を切り出して新しい文字列として取得する為のメソッドです。

### 問14
- replace()メソッド
文字列を置き換えるためのメソッド。

### 問15
- length()メソッド
文字数を戻すメソッド。

### 問16
- startsWith()メソッド
文字列が引数で指定された文字ではじまるかどうかを調べるメソッド。

### 問17
- concat()メソッド
インスタンスが保持する文字列を、引数として渡された文字列と連携し、新しい文字列を戻すメソッド。

### 問18
- stringBilder()メソッド
StirngがImmutableな一方で、StringBuilderは再代入の際に参照先の値を置き換えます。
要するに、Stringのメソッドが元々の文字列を変化させないのに対して、StringBuilderのメソッドは元々の文字列をを変えてしまう。

### 問19
- appendメソッド
文字列に新しい文字列を追加するメソッドです。
追加された文字字列は既存の文字列の後ろに追加される。

- revereメソッド
文字列を反転するメソッド。

- replaceメソッド
第１引数と第２引数で指定した範囲の文字列を、第３引数の文字列に置き換えるメソッド。

### 問20
- indexOb()メソッド
文字列の開始位置を調べるメソッド。

### 問21
- テキストブロック
トリプルクォーテーション（"""）で文字列リテラルを囲んだ
+演算子を使った複数行の連結が不要
ダブルクォーテーションのエスケーション（¥"）が不要
改行文字（¥n）も不要

### 問22
トリプルクォーテーションに改行がないとコンパイルエラー
末尾は改行は不要

### 問24
- ==演算子
同一

- equalsメソッド
同値
同じ文字列リテラルがプログラム内に再び登場すれば、インスタンスの参照が使い回される

### 問25
- new演算子
「新しいインスタンス」を作成
それぞれの変数が異なる参照を持つ

### 問26
- intern()メソッド
コンスタントプールを含むメモリ内の文字列を探して再利用するためのメソッド

### 問27
- 配列
同じ型、もしくは互換性のある型の値しか扱えない
扱える要素数はインスタンス生成時に決める。後で要素数を変えることはできない

- 配列インスタンスの生成
new int[3];

- 配列型の宣言
int[] array;

- println()メソッド
引数に渡された値をコンソールに表示するメソッド
参照先のtoString()メソッドを呼び出す　ｰ> ハッシュコードを組み合わせた値を戻す

### 問28
- 配列型の宣言①
int[] array;

- 配列型の宣言②
int array[];

配列は複数の参照をひとまとめにして扱うことができる
配列そのものもインスタンスであり、参照を通して扱える

- ２次元配列宣言
int[][] arrayA;
int[] arrayA[];

-３次元配列宣言
int arrayB[][][];
int[][] arrayB[];

### 問29
配列型変数には、配列インスタンスへの参照を代入するだけで、参照先の配列が幾つの要素を扱えるかは関係ない
- 配列型変数の宣言（コンパイルエラー）
int[2] array;

### 問30
配列インスタンスの生成には、要素数を指定しなけれならない
int型の要素数は整数値で指定
- 要素数を指定しなかった場合（コンパイルエラー）
int[] array = new int[];

### 問31
【配列の要素のデフォルト値】
|  型  |  デフォルト値  |
|----------|---------------|
| 整数型     | 0 |
| 浮動小数点数型    | 0.0 |
|  真偽値   | false |
|  文字型   | ¥u0000 |
|  オブジェクト型   | null |

- 配列の要素がデフォルト値で初期化
int[] array = new int[3]; <- 配列インスタンスを作っただけ
System.out.println(array[0]);

- 初期化子を利用して要素の値を初期化
int[] array = {10, 20, 30};

- NullPointerException
変数の参照先がない

### 問32


### 問33
- 配列インスタンスの生成・初期化と配列型変数の宣言と参照代入①
int[] array = {2, 3};

- 配列インスタンスの生成・初期化と配列型変数の宣言と参照代入②
int[] array = new int[]{2, 3};
newと初期化子の両方を使った場合、要素数を指定しない

- 配列の要素数を指定した場合（コンパイルエラー）
int[] array = new int[2]{2, 3};

- 要素数ゼロの配列インスタンスの生成
int[] array = {};
int[] array = new int[0];

- 多次元配列の生成・宣言・初期化
int[][] array = {{2, 3}}, {{4, 5}};
int[][] array = new int[][]{};
int[][] array = {};

- 多次元配列の生成・宣言・初期化（コンパイルエラー）
int[][] array = new int[]{};

### 問34
２次元目以降の配列の要素数を揃える必要がない。

### 問35
- スーパークラス型の配列型変数にサブクラスのインスタンスへの参照を代入
Object[] obj = {"A", "B", "C"}
StringクラスはObjectクラスを継承している

- AインタフェースとAをimplementsしたBクラス
public interface A {}
public class B implements A {}

- A型の配列型変数にB型の配列インスタンスの参照を代入
A[] array = new B[]{ new B (), new B () };

### 問36
- cloneメソッド
同じ値を持った配列インスタンスが複製される

### 問37
- コレクション
何らかの集合のこと。
同じ型、もしくは互換性のある型しか扱えない
扱える要素数を最初に決めなくてはいけない
要素アクセスには添字を使わなければならない
要素アクセスの際には、要素数を超えないよう配慮しなければいけない

- ArrayListクラス
オブジェクトであればどんな型でも扱える
必要に応じて要素数を自動的に増やす
追加した順に並ぶ
nullも値として追加できる
重複した値も追加できる
スレッドセーフではない

### 問38
コレクションAPIはオブジェクトであればどのような型の集合でも扱える。
object,String,Integerと３つの種類が混在するコレクションも扱える
- リストへの追加
ArrayList list = new ArrayList();
list.add(new Object());
list.add("test");
list.add(new Integer(10));

### 問39
- add()メソッド
ArrayListに要素を追加するにはaddメソッドを使う。
addメソッドの引数で渡された要素をリストの後ろに追加するメソッド。

- addメソッドでリストへの追加の場所を指定
list.add(2, "B");
文字列「B」を２番目に追加する。

### 問40
- set()メソッド
置き換えをするためのメソッド。
元の要素を上書きする

### 問41
- removeメソッド
リストから要素を削除する。

### 問42

### 問43

### 問44
- 固定長のリストを作る方法
ArrayクラスのasListメソッドを使い、配列からリストのインスタンすを生成する
Listインターフェースのofメソッドを使いインスタンスを生成する
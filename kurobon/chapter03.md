# 第3章　メモ📝
### 問１
### 問2
### 問3
### 問4
### 問5
### 問6
### 問7
### 問8
### 問9
### 問10
### 問11
- equalsメソッド
同値性を確認するメソッド
null以外の参照値Xがあったとき、x.equals(x)はtrueを返す
null以外の参照値Xとyがあったとき、y.equals(x)がtrueを返す場合は、x.equals(y)はtrueを返す
null以外の参照値xとy,zがあったとき、x.equals(y)がtrueを返し、y.equals(z)がtrueを返す場合は、x.equals(z)はtrueを返す
null以外の参照値xとyがあったとき、x.equals(y)を複数回呼び出しても、比較で使われた情報が変更されていなければ、一貫してtrueを返すか、一貫してfalseを返す
null以外の参照値xについて、x.equals(null)はfalseを返すこと
nullがあればfalseを返す

### 問12
- ビット演算
コンピュータの基本的な情報単位であるビット（０または１）を直接操作する演算のこと
ハードウェアの制御やデータ圧縮、データの暗号化などの分野で利用されている

【ビット演算子】
| 演算子 | 演算 | 説明 |
|-------|-----|-----|
| & | AND | 両方のビットが1の場合に１を返す |
| | | OR | 片方または両方のビットが1の場合に１を返す |
| ^ | XOR | 片方のビットが1の場合に１を返す |
| ~ | NOT | ビットを反転させる |
| << | 左シフト | ビットを左にシフトする |
| >> | 右シフト | ビットを右にシフトする |
| >>> | ゼロ埋め右シフト | 符号に関係なく、左側をゼロで埋めながら右にシフトする |

### 問13
### 問14
{}（中カッコ）は省略可能で省略した場合は次の一文だけがif文の条件に合致して時の処理として実行される。

### 問15
### 問16
### 問17
- switch文の条件式が戻せる型
char,short,Character,Short,String,byte,int,Byte,inreger,Enum
int型以下の整数型とっそのラッパー
文字と文字列
列挙型

long,double,float,booleanは含まれない
### 問18
### 問19
### 問20
- seitch文
defaultはどこに書いてもいい

### 問21
switch式はbreakを記述しなくていい

### 問22
switch文ではdefaultは必須ではない
switch式では必須
### 問23
### 問24
switch式の矢印以降に記述できるのは、
- 値を戻す式
- 例外をスローするthrow文
- ブロック
### 問25
### 問26
### 問27
### 問28
### 問29
### 問30
### 問31
for文
初期化文、更新文は「,」で複数記述できる
条件文は&&で記述する

### 問32
①変数iが宣言され、0で初期化される
②条件文（i < 3）が判定される
③コンソールに変数iの値が表示される
④変数iがインクリメントされる
⑤periodメソッドが呼び出される
⑥②に戻る

### 問33
### 問34
### 問35
条件式がtrueだったら常にtrueになり無限ループになる

### 問36
- 拡張for文
for ( 要素の型 任意の変数名 : 配列変数名 ) {
    // 繰り返し処理
}

### 問37
### 問38
do {
    処理
} while(条件式);

### 問39
### 問40
### 問41
### 問42
- ラベル
コードブロック
全てのループ分と分岐（if, switch）
式
代入
return文
try文
throw文

### 問43
